import 'package:flutter/material.dart';

class SettingsPageContact extends StatelessWidget {
  const SettingsPageContact({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
